<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel="icon" href="/svgs/icon.svg" type="image/x-icon">
  <title>
    <%= breed.name %>
  </title>
</head>

<body>
  <%- include('../general/navBar') %>
    <main class="flex flex-col gap-8">
      <img class="self-center size-96 rounded-full shadow-blurry sm:size-64" src="<%= breed.img_url%>"
        alt="Picture of <%= breed.name %>">
      <h1 class="title">
        <%- breed.name %>
      </h1>
      <p class="text-xl sm:text-lg sm:text-center">
        <%- breed.description %>
      </p>
      <ul class="flex flex-col gap-4 text-lg sm:text-base">
        <li>
          <span class="font-bold">Size: </span>
          <a href="<%=breed.size.url%>" class="underline-offset-4 hover:underline">
            <%- breed.size.name %>
          </a>
        </li>
        <li>
          <span class="font-bold">Life Expectancy: </span>
          <span>
            <%= breed.life_expectancy %> years
          </span>
        </li>
      </ul>
      <h2 class="text-2xl font-bold">Dogs - <%= breedDogs.length %>
      </h2>
      <ul class="grid grid-cols-[repeat(auto-fit,calc(224px+4rem))] justify-center gap-4">
        <% breedDogs.forEach((dog)=> { %>
          <li class="flex flex-col p-8 sm:p-4">
            <a class="round-link" href="<%= dog.url %>">
              <img class="round-img" src="<%= dog.img_url%>" alt="Picture of <%= dog.name %>">
            </a>
            <span class="self-center text-lg mt-6">
              <%= dog.name %>
            </span>
          </li>
          <% }); %>
      </ul>
      <div id="buttons" class="flex justify-center gap-20 sm:flex-col sm:gap-10">
        <a class="button border-blue-700  text-blue-700 hover:bg-blue-700" href=<%=`${breed.url}/update`%> >Update
          Breed</a>
        <a class="button border-red-700 text-red-700 hover:bg-red-700" href=<%=`${breed.url}/delete`%>
          >Delete Breed</a>
      </div>
    </main>
    <%- include('../general/addendum') %>
</body>

</html>