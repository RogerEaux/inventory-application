<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Breeds</title>
</head>

<body>
  <%- include('../general/navBar') %>
    <main class="flex flex-col gap-8">
      <h1 class="text-4xl font-bold">Breeds</h1>
      <ul class="grid grid-cols-[repeat(auto-fit,calc(224px+4rem))] justify-center gap-4">
        <% breedList.forEach((breed)=> { %>
          <li class="flex flex-col p-8">
            <a class="round-link" href=" <%=breed.url %>">
              <img class="round-img" src="<%-breed.img_ur %>" alt="Picture of a <%= breed.name %>">
            </a>
            <span class="self-center text-lg mt-6">
              <%= breed.name %>
            </span>
          </li>
          <% }); %>
      </ul>
      <a class="button border-green-700 text-green-700 hover:bg-green-700" href="/inventory/breed/create">Create
        Breed</a>
    </main>
    <%- include('../general/addendum') %>
</body>

</html>