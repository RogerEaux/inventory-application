<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>Breeds</title>
</head>

<body>
  <%- include('../general/navBar') %>
    <main class="flex flex-col gap-8 px-16 py-8">
      <h1 class="text-4xl font-bold">Breeds</h1>
      <ul class="grid grid-cols-[repeat(auto-fit,calc(224px+4rem))] justify-center gap-4">
        <% breedList.forEach((breed)=> { %>
          <li class="flex flex-col p-8">
            <a class="size-56 rounded-full shadow-blurry overflow-clip" href=" <%=breed.url %>">
              <img class="rounded-full object-cover transition-all duration-500 hover:scale-125"
                src="<%-breed.img_ur %>" alt="Picture of a <%= breed.name %>">
            </a>
            <span class="self-center text-lg mt-6">
              <%= breed.name %>
            </span>
          </li>
          <% }); %>
      </ul>
      <a class="self-center py-3 px-6 border-4 border-solid border-green-700 rounded-lg font-bold text-lg text-green-700 transition-all duration-300 hover:bg-green-700 hover:text-white"
        href="/inventory/breed/create">Create Breed</a>
    </main>
    <%- include('../general/addendum') %>
</body>

</html>